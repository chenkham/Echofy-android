<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_compact_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background">

    <!-- Full-bleed background album art -->
    <ImageView
        android:id="@+id/widget_compact_background_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:clickable="false"
        android:focusable="false"
        android:contentDescription="@null" />

    <!-- Dark gradient / scrim overlay using ImageView for RemoteViews support -->
    <ImageView
        android:id="@+id/widget_compact_scrim_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:clickable="false"
        android:focusable="false"
        android:background="#66000000"
        android:contentDescription="@null" />

    <!-- Main Content Layout -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="14dp">

        <!-- Top-left Waveform / Equalizer glyph -->
        <ImageView
            android:id="@+id/widget_compact_waveform_icon"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:src="@drawable/echofy"
            android:contentDescription="@null" />

        <!-- Circular White Play/Pause Button on Right -->
        <ImageButton
            android:id="@+id/widget_compact_play_pause"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@drawable/widget_play_circle_white"
            android:src="@drawable/play"
            android:padding="11dp"
            android:scaleType="fitCenter"
            android:contentDescription="Play or pause" />

        <!-- Middle Song Title & Artist -->
        <LinearLayout
            android:id="@+id/widget_compact_info_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@id/widget_compact_play_pause"
            android:layout_marginEnd="12dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/widget_compact_song_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="marquee"
                android:singleLine="true"
                android:text="@string/song_title"
                android:textColor="#FFFFFFFF"
                android:textSize="15sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/widget_compact_artist"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="marquee"
                android:singleLine="true"
                android:text="@string/artist_name"
                android:textColor="#B3FFFFFF"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- Bottom Controls Row (Prev, Progress, Next) -->
        <LinearLayout
            android:id="@+id/widget_compact_controls"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/widget_compact_prev"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:padding="5dp"
                android:src="@drawable/skip_previous"
                android:contentDescription="Previous" />

            <ProgressBar
                android:id="@+id/widget_compact_progress_bar"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="0dp"
                android:layout_height="3dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:max="100"
                android:progress="0" />

            <ImageButton
                android:id="@+id/widget_compact_next"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:padding="5dp"
                android:src="@drawable/skip_next"
                android:contentDescription="Next" />
        </LinearLayout>
    </RelativeLayout>
</FrameLayout>
